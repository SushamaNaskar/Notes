# Prettier
<!-- Prettier is a code formatter that automatically formats code by handling indentation, spacing, line breaks, and styling rules. It ensures that all code follows a consistent format across the project. -->
- Prettier is a code formatter, It ensures that all code follows a consistent format across the project.