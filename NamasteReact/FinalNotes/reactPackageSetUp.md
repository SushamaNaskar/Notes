# Adding packages (react, react-dom and parcel packages)
- Install node.js
- config npm (npm init: creates package.json)
- Install react and react-dom packages (creates package-lock.json and node_modules)
- Install parcel(a bundler)
- start the application (npx parcel root-file-name)

